{
  "name": "myddle",
  "description": "Context carrying Middleware for regular functions, with error handlers.",
  "version": "0.5.0",

  "main": "myddle.js",

  "devDependencies": {
    "tape": "~2.3.0"
  },

  "scripts": {
    "test": "set -e; for FILE in test/*.js; do echo \"=== $FILE ===\"; node $FILE; done"
  },

  "licenses" : [{
    "type" : "MIT",
    "url" : "http://chilts.mit-license.org/2013/"
  }]
}
